# Medical Instructions RAG - Environment Configuration
# Copy this file to .env and adjust values as needed

# Ingestion Configuration
# Limit the number of items to process (default: 100 for testing)
# Set to 0 or leave unset to process all items
INGESTION_LIMIT=100

# Rate limiting: delay in seconds between HTTP requests (default: 0.5)
REQUEST_DELAY=0.5

# File Paths
# Path to the CSV registry file
CSV_PATH=reestr.csv

# Directory for downloaded HTML instruction files
HTML_DIR=data/html

# Directory for downloaded MHT instruction files
MHT_DIR=data/mht

# ChromaDB Configuration
# Directory for ChromaDB persistent storage
CHROMA_DIR=storage/chroma

# ChromaDB collection names
# Collection for medicine metadata (ID, names, status, file paths)
CHROMA_MEDICINES_COLLECTION=medicines

# Collection for RAG document chunks (instruction content with embeddings)
# This will be used in Phase 2 for semantic search
CHROMA_RAG_COLLECTION=instruction_chunks

# OpenAI Configuration (for Phase 2 - RAG)
# Your OpenAI API key (required for Phase 2)
OPENAI_API_KEY=sk-...

# OpenAI model for chat/completion (default: gpt-4o-mini - cheap and supports Ukrainian)
OPENAI_LLM_MODEL=gpt-4o-mini

# OpenAI model for embeddings (default: text-embedding-3-small - cheap and supports Ukrainian)
OPENAI_EMBED_MODEL=text-embedding-3-small

# Indexing Configuration (Phase 2)
# Chunk size for text splitting (default: 1000 characters)
CHUNK_SIZE=2000

# Chunk overlap for context preservation (default: 200 characters)
CHUNK_OVERLAP=200

# Limit number of files to index (default: 0 = process all)
INDEXING_LIMIT=0
